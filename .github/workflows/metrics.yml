name: Metrics

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Generate metrics
        uses: lowlighter/metrics@v3.28
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          user: AlanValdevenito
          base: header, activity
          plugin_languages: yes
          plugin_isocalendar: yes
